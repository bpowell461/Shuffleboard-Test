var i = 0;
var txt = 'BinaryTree bst = new BinaryTree';
var speed = 30;

function typeWriter() {
  if (i < txt.length) {
    document.getElementById("codeType").innerHTML += txt.charAt(i);
    i++;
    setTimeout(typeWriter, speed);
  }
}
